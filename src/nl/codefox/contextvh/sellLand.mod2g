use knowledgebase as knowledge.
use tygron as actionspec.

exit=always.

% The selling land module
module sellLand {
	% we do not know what kind of land is available so we will need to hardcode a location
	%if true then map_sell_land(BuyerID, square(a, b, c, d), 100).
}
