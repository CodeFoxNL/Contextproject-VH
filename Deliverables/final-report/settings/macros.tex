% Make lists have no vertical seperation, for really tight lists
\setlist{nolistsep}

% Text on intentionally blank pages
\newcommand*{\blankpage}{%
	\begingroup
	\setlength{\topskip}{0pt}%
	\vspace*{\fill}%
	\nointerlineskip % suppresses partial \baselineskip above text
	\hbox to \linewidth{%
		\hbox to \textwidth{%
			\hfill
			This page is intentionally left blank.
			\hfill
		}%
		\hss
	}%
	\vspace{\fill}%
	\endgroup
	% \showlists
}
\makeatletter
\renewcommand*{\cleardoublepage}{\clearpage\if@twoside \ifodd\c@page\else
	\blankpage
	\thispagestyle{empty}
	\newpage
	\if@twocolumn\hbox{}\newpage\fi\fi\fi}
\makeatother

% Commands for setting and resetting numbering of pages
\newcommand{\newroman}{
	\renewcommand{\thepage}{\roman{page}}
	\setcounter{page}{1}
}
\newcommand{\newarabic}{
	\renewcommand{\thepage}{\arabic{page}}
	\setcounter{page}{1}
}

\newcolumntype{L}[1]{>{\raggedright\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}
\newcolumntype{C}[1]{>{\centering\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}
\newcolumntype{R}[1]{>{\raggedleft\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}

\raggedbottom

\definecolor{gray75}{gray}{0.75}
\newcommand{\hsp}{\hspace{20pt}}
\titleformat{\chapter}[hang]{\Huge\bfseries}{\thechapter\hsp\textcolor{gray75}{|}\hsp}{0pt}{\Huge\bfseries}

\newcommand{\super}{\textsuperscript}

% FancyHDR Settings
\fancypagestyle{plain} {
	\fancyhf{}
	\fancyfoot[C]{Final Report Context Project Virtual Humans\\Team CodeFox}
	\fancyfoot[R]{\thepage}
	\renewcommand{\headrulewidth}{0pt}
	\renewcommand{\footrulewidth}{1pt}
}
\fancypagestyle{maincontent} {
	\fancyhf{}
	\fancyhead[LE,RO]{\leftmark \\}
	\fancyhead[RE,LO]{\rightmark}
	\fancyfoot[C]{Final Report Context Project Virtual Humans\\Team CodeFox}
	\fancyfoot[R]{\thepage}
	\renewcommand{\headrulewidth}{2pt}
	\renewcommand{\footrulewidth}{1pt}
}